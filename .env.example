# Copy this file to .env and fill in real values. Never commit .env itself.

# The deployed backend's URL, e.g. https://meal-finder-api.onrender.com/api
# Leave unset for local dev — it falls back to http://localhost:5050/api.
VITE_API_URL=
